{
  "buildCommand": "echo \"skip build\"",
  "installCommand": "echo \"skip install\"",
  "outputDirectory": "build/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}