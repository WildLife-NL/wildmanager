# Kopieer naar `.env` en vul je waarden in (`.env` staat niet in git).
DEV_BASE_URL=https://test-api-wildlifenl.uu.nl

# Optioneel: zet op true of 1 om dev-debugtools op de kaart te tonen
DEV_DEBUG_TOOLS=false
